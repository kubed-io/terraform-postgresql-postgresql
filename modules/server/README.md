# PostgreSQL Server

A module for provisioning all roles that should exist on a PostgreSQL server. Iterates a list of roles and delegates each to the `../role` module.

## References

- [OpenTofu Postgresql Provider Docs](https://search.opentofu.org/provider/cyrilgdn/postgresql/latest/docs/resources/postgresql_role)
